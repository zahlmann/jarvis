prefix_rule(
    pattern = ["git", "push", "origin", "main"],
    decision = "allow",
    justification = "Allow routine main-branch pushes for this repository",
    match = [
        "git push origin main",
    ],
    not_match = [
        "git push origin feature/test",
        "git push upstream main",
        "git push",
    ],
)
